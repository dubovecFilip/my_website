---
/* Netlify servíruje /404.html pri neznámej ceste. Bez jazykového prefixu
   ukážeme predvolenú mutáciu; skript v komponente ponúkne druhý jazyk. */
import NotFound from '../screens/NotFound.astro';
import { defaultLang } from '../i18n/dict';
---

<NotFound lang={defaultLang} />
